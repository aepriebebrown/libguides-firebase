This is the github repo for a web app that searches the MSU Libraries LibGuides via an API. View the live app here: https://libguides-679c9.firebaseapp.com/

The app uses firebase for user authentication. The search functionality and DOM manipulation is written in vanilla JavaScript and uses Axios to handle API requests.
